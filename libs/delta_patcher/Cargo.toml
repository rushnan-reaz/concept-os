[package]
name = "delta_patcher"
version = "0.1.0"
edition = "2021"

[features]
default = ["std"]
std = []       # gates encoder.rs + Vec-based APIs (host only)

[dependencies]
# none — pure no_std core
